spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/duneinsolite
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        #dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
keycloak:
  admin:
    server-url: http://localhost:8180
    realm: duneinsolite
    client-id: duneinsolite-api
    client-secret: DzXfwEHyTCynUJtKevpCXlz5yVtBBUYK  # paste your client secret here
  realm: duneinsolite

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/duneinsolite

server:
  port: 8080